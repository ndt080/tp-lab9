import SwiftUI
import CoreData

struct DontBookingView: View {
    
    var body: some View {
        Form {
            Section(header: Text("Booking a ticket")) {
                Text("No routes selected")
            }
        }
    }
}
